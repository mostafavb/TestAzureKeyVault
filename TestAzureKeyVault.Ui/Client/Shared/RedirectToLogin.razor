@inject NavigationManager Navigation

<h3>Checking login state</h3>

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateToLogin($"authentication/login?returnUrl={Uri.EscapeDataString(Navigation.Uri)}");
    }
}
